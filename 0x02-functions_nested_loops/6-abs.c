#include "main.h"

/**
 * _abs - prints the absolute value of an integer.
 * @n : is the value to be printed.
 *
 * Return: absolute integer value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
