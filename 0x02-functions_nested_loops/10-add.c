#include "main.h"
#include <stdio.h>
/**
 * add - Adds two integers.
 * @x: the first integer
 * @y: the second integer
 *
 * Return: 0
 */
int add(int x, int y)
{
	int sum = x + y
		printf("%d\n", sum);
	return (0);
}
